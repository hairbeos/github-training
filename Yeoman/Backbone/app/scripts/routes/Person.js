/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var PersonRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return PersonRouter;
});